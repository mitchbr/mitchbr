import 'package:flutter/material.dart';

class ChecklistView extends StatelessWidget {
  const ChecklistView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Checklist'));
  }
}
